import 'package:flutter/material.dart';

class ThemeDataCustom extends StatelessWidget {
  const ThemeDataCustom({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
